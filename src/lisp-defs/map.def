(defcppclass Map ("<root.h>" "<layer.h>" "<vector>" "<string>" "<texture.h>" "<resize-enums.h>")
  (declare-class "root" "Layer" "Texture")
  (public 
   (properties
    (std__string name "Map 1")
    (Texture* text nullptr)
    (std__vector<Layer*>* layers nullptr))
   (functions
    (void parent (root* p))
    (int width ())
    (int height ())
    (void resize (int w
		  int h
		  verticalAnchor vAnchor
		  horizontalAnchor hAnchor))
    (root* parent ()))))  
