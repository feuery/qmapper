(defcppclass Map ("<root.h>" "<layer.h>" "<vector>" "<string>")
  (declare-class "root" "Layer")
  (public
   (fields
    (std__vector<Layer*>* layers nullptr))
   (properties
    (std__string name "Map 1"))
   (functions
    (void parent (root* p)))
   (functions
    (int width (void ))
    (int height (void))
    (root* parent (void))))) 
