(defcppclass Map ("<root.h>" "<layer.h>" "<vector>" "<string>" "<texture.h>")
  (declare-class "root" "Layer" "Texture")
  (public
   (fields
    (std__vector<Layer*>* layers nullptr)) 
   (properties
    (std__string name "Map 1")
    (Texture* text nullptr))
   (functions
    (void parent (root* p)))
   (functions
    (int width ())
    (int height ())
    (root* parent ()))))  
