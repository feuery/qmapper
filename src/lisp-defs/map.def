(defcppclass Map ("<root.h>" "<layer.h>" "<vector>" "<string>" "<resize-enums.h>" "<animatedSprite.h>")
  (declare-class "root" "Layer" "Texture")
  (public 
   (properties
    (std__string name "Map 1")
    (std__vector<Layer*>* layers nullptr))  
   (functions
    (either<animatedsprite*___Sprite*> findNearest (int x int y))
    (void parent (root* p))
    (int width ())
    (int height ())
    (void resize (int w
		  int h
		  verticalAnchor vAnchor
		  horizontalAnchor hAnchor))
    (root* parent ()))))  
