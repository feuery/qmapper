(defcppclass Layer ("<tile.h>" "<string>" "<vector>" "<map.h>")
  (declare-class "Map" "Tile")
  (public
   (fields
    (std__vector<std__vector<Tile>>* tiles nullptr))
   (properties
    (std__string name ""))
   (functions
    (int getWidth (void))
    (int getHeight (void)))
   (functions
    (void set_parent (Map* p)))
   (functions
    (Map* parent (void))))
  (protected
   (fields
    (int w 0)
    (int h 0)
    (Map* p nullptr)))) 
  
