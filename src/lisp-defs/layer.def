(defcppclass Layer ("<tile.h>" "<string>" "<vector>" "<map.h>")
  (declare-class "Map" "Tile")
  (public
   (fields
    (std__vector<std__vector<Tile>>* tiles nullptr))
   (properties
    (std__string name "")
    (unsigned_char opacity 0)
    (bool visible false)) 
   (functions
    (void set_parent (Map* p))
    (int getWidth ())
    (int getHeight ())
    (Map* parent ())))
  (protected
   (fields
    (int w 0)
    (int h 0)
    (Map* p nullptr)))) 
  
